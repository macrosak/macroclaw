[test]
coverage = true
# Function coverage disabled — bun/JSC reports phantom functions (e.g. citty defineCommand). Lines are what matter.
coverageThreshold = { functions = 0.0, lines = 1.0 }
preload = ["./src/test-setup.ts"]
